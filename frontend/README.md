# AI PDF Assistant — Frontend

React + Vite client for chatting with uploaded PDFs via the FastAPI RAG backend.

## Setup

```bash
cp .env.example .env
npm install
npm run dev
```

Requires the backend at `VITE_API_BASE_URL` (default `http://localhost:8000`).
